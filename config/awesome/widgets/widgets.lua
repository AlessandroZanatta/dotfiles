return {
    textclock = require "widgets.clock",
    battery = require "widgets.battery",
    alsa = require "widgets.alsa",
    temp = require "widgets.temp",
    cpu = require "widgets.cpu",
    memory = require "widgets.memory",
    systray = require "widgets.systray",
}
